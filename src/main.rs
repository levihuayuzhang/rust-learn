use std::io;

fn main() {
    // println!("Guess:");

    // let mut guess = String::new();
    let x = 5;
    let y = 7;

    // io::stdin()
    //     .read_line(&mut guess)
    //     .expect("Failed to read line...");

    println!("x = {x}, y = {}", y*2);

    // print!("You guessed: {guess}");
}
